[package]
name = "determinism-checker"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[lints]
workspace = true

[dependencies]
quote = "1"
syn = { version = "2", features = ["full", "visit"] }
